# Properties to be used across all the environments. They can be overwritten.

# in miliseconds
poll.frequency=5000
poll.delay=0

watermark.default.expression=#[groovy: new java.util.Date(System.currentTimeMillis() - 1000 * 60)]

# in miliseconds 
msdyn.offset=3000